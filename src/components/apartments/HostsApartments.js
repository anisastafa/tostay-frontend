import React from "react";
import {makeStyles} from "@material-ui/core";

const useStyles = makeStyles(() => ({

}));

const HostsApartments = () =>{
    const { text } = useStyles();
    return (
        <div className={text}>
            <h1>hosts' apartments</h1>
            <h1>hosts' apartments</h1>
            <h1>hosts' apartments</h1>
        </div>
    )
};

export default HostsApartments;